% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refit_rtm.R
\name{refit_rtm}
\alias{refit_rtm}
\title{Re-estimate a fitted risk terrain model on new events}
\usage{
refit_rtm(x, new_events, new_offset = NULL, level = 0.95)
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{new_events}{An sf or sfc object with the new outcome event points,
in the same CRS as the model grid.}

\item{new_offset}{Replacement denominator for offset models: an sf/sfc
point layer (counted per cell) or a numeric vector with one value per
grid cell. If \code{NULL} (default) the original denominator is reused,
which is appropriate for same-period thinned splits where only the
outcome events were split.}

\item{level}{Confidence level for the refitted RRV intervals (default
0.95).}
}
\value{
An object of class \code{rtm_refit}: a list with \code{selected} (the
selected-variables table comparing \code{rrv_original} with \code{rrv_refit}
and its \code{ci_lower}/\code{ci_upper} and \code{p_refit}), \code{adjustments} (original
vs refitted coefficients of adjustment covariates, \code{NULL} if none),
\code{intercept} (refitted), \code{family}, \code{n_events}, \code{n_cells} (cells used
in the refit), \code{converged}, and \code{level}.
}
\description{
Takes the \emph{frozen} specification of a fitted \code{\link[=rtm]{rtm()}} model — the
selected variables, adjustment covariates, offset structure, and
distribution family — and re-estimates its coefficients on a new set of
outcome events, without repeating any selection. Because the new data
played no part in choosing the specification, the refitted p-values and
confidence intervals are ordinary ones, free of the post-selection
caveat attached to the original fit.
}
\details{
What a difference between original and refitted RRVs \emph{means} depends on
where the new events come from:
\itemize{
\item \strong{Same period, thinned split} (see \code{\link[=split_events]{split_events()}}): temporal change
is excluded by construction, so systematic attenuation of the
refitted RRVs towards 1 is the signature of selection bias (winner's
curse) in the original estimates.
\item \strong{A later period} (temporal holdout): differences combine selection
bias \emph{and} genuine change in the effects over time. This is the
relevant quantity for prospective use ("do these effects hold now?"),
but it cannot separate the two sources; running both designs can —
attenuation already present in the thinned split is selection, any
additional change in the temporal refit is drift.
}

The refit keeps the original grid, operationalized risk factor
variables, and cell-level covariates: only the outcome counts (and
optionally the offset denominator) are replaced. Risk factor layers are
therefore assumed unchanged; if the environment itself changed between
periods (new bars, closed clinics), rebuild the model instead. The
distribution family is kept fixed; for a negative binomial model the
dispersion parameter is re-estimated on the new data.
}
\seealso{
\code{\link[=split_events]{split_events()}} to create a same-period split,
\code{\link[=validate_rtm]{validate_rtm()}} for the discrimination side of holdout checking.
}

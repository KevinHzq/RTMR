% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rtm_stepwise.R
\name{rtm_stepwise}
\alias{rtm_stepwise}
\title{Bidirectional stepwise selection of risk factors by BIC}
\usage{
rtm_stepwise(
  data,
  outcome,
  meta,
  family = c("poisson", "nb"),
  alpha_level = 0.05,
  offset_col = NULL,
  always = character(0),
  verbose = FALSE
)
}
\arguments{
\item{data}{A data.frame with the outcome counts and candidate binary
variables per grid cell.}

\item{outcome}{Name of the outcome count column in \code{data}.}

\item{meta}{Variable metadata data.frame (columns \code{variable} and \code{factor}
at minimum), as attached by \code{\link[=operationalize]{operationalize()}}; only variables listed
here are candidates.}

\item{family}{\code{"poisson"} or \code{"nb"} (negative binomial, via
\code{\link[MASS:glm.nb]{MASS::glm.nb()}}).}

\item{alpha_level}{Significance level for the validity check (default 0.05).}

\item{offset_col}{Optional name of a column in \code{data} holding the model
offset on the log scale (e.g. log of denominator event counts).}

\item{always}{Character vector of adjustment covariate columns included
in every candidate model (starting with the null model). They do not
compete for selection, may take either coefficient sign, and are not
subject to the significance check.}

\item{verbose}{Print progress messages (default \code{FALSE}).}
}
\value{
A list with elements \code{fit} (the final model object), \code{variables}
(selected variable names, excluding \code{always} terms), \code{bic}, and
\code{family}, or \code{NULL} if even the null model could not be fitted.
}
\description{
Builds a parsimonious risk terrain model as in the RTMDx Utility: starting
from a null model, variables are added and removed one step at a time,
keeping the step with the best (lowest) Bayesian information criterion,
until no valid step improves the score. RTMDx's entry rules are enforced:
at most one variable (i.e. one operationalization and spatial influence)
per risk factor may be in the model, all coefficients must be positive
(congruent with the coded aggravating/protective direction), and all
variables in a candidate model must be significant at \code{alpha_level}.
Note that, as in RTMDx, significance is a sanity check only; steps are
chosen by BIC, so reported p-values should not be read as regular p-values.
}

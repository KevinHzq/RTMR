% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stability_rtm.R
\name{stability_rtm}
\alias{stability_rtm}
\title{Bootstrap the selection pipeline: stability and bagged estimates}
\usage{
stability_rtm(
  x,
  nboot = 200,
  resample = c("cell", "block"),
  cluster_size = NULL,
  nfolds = 5,
  alpha_level = 0.05,
  level = 0.95,
  verbose = TRUE
)
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{nboot}{Number of bootstrap resamples (default 200; 50 already
gives a rough stability reading).}

\item{resample}{\code{"cell"} (default) resamples grid cells independently;
\code{"block"} resamples contiguous square tiles of cells.}

\item{cluster_size}{Tile side length for \code{resample = "block"}, in CRS
units (default: twice the largest candidate spatial influence).}

\item{nfolds, alpha_level}{Culling folds and stepwise significance level,
as in \code{\link[=rtm]{rtm()}} (they are not stored in the fitted object; use the same
values as the original call).}

\item{level}{Level for the percentile intervals (default 0.95).}

\item{verbose}{Print progress every 10\% (default \code{TRUE}).}
}
\value{
An object of class \code{rtm_stability}: a list with \code{factors} (a
data.frame with per-factor selection frequency \code{freq}, the full-data
\code{rrv_full} for selected factors, the bagged \code{rrv_bagged}, and
percentile limits \code{rrv_lo}/\code{rrv_hi}), \code{variables} (per-variable
selection frequencies), \code{nboot}, \code{n_failed} (resamples where no model
could be fitted), \code{resample}, and \code{level}.
}
\description{
Reruns the full RTMDx-style selection procedure — penalized culling
followed by bidirectional stepwise regression under both distributions —
on bootstrap resamples of the grid cells, and aggregates which factors
are selected and with what coefficients. Unlike sample splitting (see
\code{\link[=split_events]{split_events()}}), every resample uses the full data, so nothing is set
aside: this is the post-selection tool for sparse settings (few events,
rural study areas) where halving the events would starve the selection
stage.
}
\details{
Three complementary summaries are returned:
\itemize{
\item \strong{Selection frequency} (Meinshausen & Bühlmann, 2010): the share of
resamples in which each factor is selected at all, and in which each
individual variable (operationalization x spatial influence) is
chosen. A factor selected in 95\% of resamples is not a lucky artifact
of one draw; a factor selected in 30\% of them should not be presented
as a finding, whatever its p-value.
\item \strong{Bagged RRV} (Efron, 2014): the factor's coefficient averaged over
\emph{all} resamples, counting 0 when the factor was not selected, then
exponentiated. Averaging over selection shrinks lucky-high estimates
towards 1 in exactly the situations that produce the winner's curse,
giving a selection-aware point estimate without splitting the data.
\item \strong{Percentile interval}: quantiles of the per-resample coefficient
(including the zeros), reflecting both estimation and selection
uncertainty. With moderate selection frequency the interval rightly
includes 1.
}

Cells can be resampled independently (\code{resample = "cell"}) or in
contiguous spatial blocks (\code{resample = "block"}, a block bootstrap using
square tiles of side \code{cluster_size}, defaulting to twice the largest
candidate spatial influence as in \code{\link[=robust_rtm]{robust_rtm()}}). Block resampling
additionally preserves short-range spatial dependence within the
resamples and is recommended when \code{\link[=moran_rtm]{moran_rtm()}} flags autocorrelation.

A factor's coefficient may be estimated at different spatial influences
in different resamples; the aggregation pools them, and the returned
variable-level frequencies show how concentrated the choice of
influence is. Set a seed before calling for reproducibility. The
procedure refits the whole pipeline \code{nboot} times, so expect a runtime
of roughly \code{nboot} times a single \code{\link[=rtm]{rtm()}} fit (minus the
operationalization).
}
\references{
Efron, B. (2014). Estimation and accuracy after model
selection. \emph{Journal of the American Statistical Association}, 109,
991-1007.

Meinshausen, N., & Bühlmann, P. (2010). Stability selection. \emph{Journal
of the Royal Statistical Society: Series B}, 72, 417-473.
}
\seealso{
\code{\link[=split_events]{split_events()}} and \code{\link[=refit_rtm]{refit_rtm()}} for the sample-splitting
route when events are plentiful.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/robust_rtm.R
\name{robust_rtm}
\alias{robust_rtm}
\title{Cluster-robust inference for a risk terrain model}
\usage{
robust_rtm(x, cluster_size = NULL, cluster = NULL, level = 0.95)
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{cluster_size}{Side length of the square spatial clusters, in CRS
units. Defaults to twice the largest candidate spatial influence
distance in the model.}

\item{cluster}{Optional custom cluster assignment overriding the square
tiling: a vector with one value per grid cell (e.g. neighbourhood or
census tract identifiers).}

\item{level}{Confidence level (default 0.95).}
}
\value{
An object of class \code{rtm_robust}: a list with \code{selected} (the
model's selected-variables table with columns \code{rrv}, \code{se_robust} and
\code{se_model} (log scale), \code{p_robust}, \code{p_model}, and RRV-scale confidence
limits \code{ci_lower}/\code{ci_upper}), \code{adjustments} (same columns on the
coefficient scale, \code{NULL} if none), \code{n_clusters}, \code{cluster_size} (\code{NA}
if a custom \code{cluster} was supplied), and \code{level}.
}
\description{
Recomputes the standard errors, p-values, and confidence intervals of a
fitted \code{\link[=rtm]{rtm()}} model with a cluster-robust (sandwich) variance estimator,
grouping cells into square spatial clusters. Point estimates (and hence
RRVs and the risk map) are unchanged; only the uncertainty statements are
re-estimated to tolerate correlation between nearby cells.
}
\details{
The model-based standard errors assume independent cells. Under residual
spatial autocorrelation (see \code{\link[=moran_rtm]{moran_rtm()}}) they are too small and the
reported p-values anti-conservative. The cluster-robust estimator allows
arbitrary correlation \emph{within} clusters while assuming independence
\emph{between} them, so clusters should be large relative to the residual
correlation range; by default the cluster side is twice the largest
candidate spatial influence distance, so that cells exposed to the same
feature usually share a cluster. Because results can be sensitive to
this choice, report the robust intervals for two or three cluster sizes
when autocorrelation is a concern.

Note that \code{cluster_size} is unrelated to \code{block_length} in \code{\link[=rtm]{rtm()}}:
\code{block_length} is the average \emph{city block} length used to build the
ladder of candidate spatial influences, while \code{cluster_size} (like
\code{cell_size}, a side length in CRS units) defines the spatial tiles used
only for variance estimation.

Two limitations to keep in mind. First, cluster asymptotics rely on the
\emph{number of clusters}; a warning is issued below 30, where robust
p-values are themselves unreliable. Second, robust variances address
dependence, not selection: variables were still chosen by the data, so
even robust p-values inherit the post-selection caveat, and fully honest
inference additionally requires re-estimating the selected specification
on holdout data. For negative binomial models the dispersion parameter
is treated as fixed at its estimate.
}
\references{
Cameron, A. C., & Miller, D. L. (2015). A practitioner's
guide to cluster-robust inference. \emph{Journal of Human Resources}, 50,
317-372.
}
\seealso{
\code{\link[=moran_rtm]{moran_rtm()}} to diagnose whether robust inference is needed,
and \code{\link[=correlogram_rtm]{correlogram_rtm()}} to estimate \code{cluster_size} from the residuals'
spatial correlation range.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/refit_rtm.R
\name{split_events}
\alias{split_events}
\title{Randomly split events for selection-clean inference}
\usage{
split_events(pt, prop = 0.5)
}
\arguments{
\item{pt}{An sf or sfc object with the event points.}

\item{prop}{Probability that an event is assigned to the training half
(default 0.5).}
}
\value{
A list with sf/sfc elements \code{train} and \code{test}, a partition of
the rows of \code{pt}.
}
\description{
Splits an event point layer into a training and a test layer by
independently assigning each event to the training set with probability
\code{prop} (Poisson thinning, also known as count splitting; Neufeld et al.,
2024). Select the model on the training half with \code{\link[=rtm]{rtm()}}, then
re-estimate the selected specification on the test half with
\code{\link[=refit_rtm]{refit_rtm()}}: because thinning a Poisson process yields two independent
processes over the \emph{same} cells and time period, the test-half
coefficients are untainted by model selection and unaffected by temporal
change in the effects.
}
\details{
Under Poisson variation the two halves are exactly independent. Under
overdispersion (the negative binomial case, e.g. events that trigger
nearby events) they are mildly positively dependent, making test-half
inference slightly optimistic — still far closer to honest than reusing
the selection data.

Splitting halves the events available to each stage, so expect less
power in both selection and inference; \code{prop} can be raised above 0.5 to
favour selection or lowered to favour inference.
}
\references{
Neufeld, A., Dharamshi, A., Gao, L. L., & Witten, D. (2024).
Data thinning to avoid double dipping. \emph{Journal of the American
Statistical Association}, 119, 2416-2427.
}
\seealso{
\code{\link[=refit_rtm]{refit_rtm()}}
}

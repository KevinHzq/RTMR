% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smooth_offset.R
\name{smooth_offset}
\alias{smooth_offset}
\title{Smooth denominator events into a total-preserving offset surface}
\usage{
smooth_offset(pt, grid, bandwidth)
}
\arguments{
\item{pt}{An sf or sfc object with the denominator event points (e.g.
all overdose events).}

\item{grid}{An sf grid created by \code{\link[=create_grid]{create_grid()}}.}

\item{bandwidth}{Kernel bandwidth in the units of the CRS.}
}
\value{
A numeric vector with one non-negative value per grid cell,
summing to the number of denominator events within the grid.
}
\description{
Builds a smoothed per-cell denominator for \code{\link[=rtm]{rtm()}} rate models by kernel
density estimation of the denominator events (\code{\link[=compute_density]{compute_density()}},
Epanechnikov kernel), rescaled so that the cell values sum to the number
of denominator events falling in the grid (total-preserving). The result
can be passed to the \code{offset} argument of \code{\link[=rtm]{rtm()}} in place of raw
per-cell counts.
}
\details{
Intended for \emph{sensitivity analysis}, not as the primary offset. Raw
counts are statistically exact: cells with a zero denominator carry no
information about the event rate and their exclusion is harmless. A
smoothed offset instead (1) assigns synthetic denominators to cells with
no observed activity, (2) makes results sensitive to the \code{bandwidth}
choice, and (3) blurs denominator mass across exposure boundaries while
the outcome counts stay point-accurate, which can bias the RRVs of
factors co-located with activity peaks. Recommended use: fit the model
with raw counts as the primary analysis, refit with smoothed offsets at
two or three bandwidths, and report the stability of the RRVs.
}
\seealso{
\code{\link[=rtm]{rtm()}}, \code{\link[=compute_density]{compute_density()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correlogram_rtm.R
\name{correlogram_rtm}
\alias{correlogram_rtm}
\title{Residual spatial correlogram and cluster-size estimation}
\usage{
correlogram_rtm(
  x,
  breaks = NULL,
  nsim = 199,
  type = c("pearson", "deviance"),
  alpha_level = 0.05
)
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{breaks}{Increasing positive numeric vector of band upper edges, in
CRS units; the bands are \verb{(0, breaks[1]], (breaks[1], breaks[2]], ...}.
Default: one-cell-width steps up to \verb{3 * max(spatial influence)},
capped at 15 bands.}

\item{nsim}{Number of residual permutations per band (default 199).}

\item{type}{Residual type, \code{"pearson"} (default) or \code{"deviance"}.}

\item{alpha_level}{Significance level used to call a band correlated
when estimating the range (default 0.05).}
}
\value{
An object of class \code{rtm_correlogram}: a list with \code{bands} (a
data.frame with the band edges, number of cell pairs, Moran's I,
permutation envelope bounds \code{null_lo}/\code{null_hi} at \code{1 - alpha_level},
and permutation p-value), \code{range} (estimated correlation range; 0 if
the first band is already non-significant, \code{Inf} if every band is
significant), \code{suggested_cluster_size} (equal to \code{range} when finite
and positive, otherwise \code{NA}), \code{expected} (null expectation of I),
\code{n_cells}, \code{nsim}, and \code{alpha_level}.
}
\description{
Computes Moran's I of the model residuals within successive distance
bands (a spatial correlogram) to estimate how far residual spatial
correlation reaches. The estimated range is the distance at which the
band-wise Moran's I decays to the permutation null, and is the
data-driven choice for \code{cluster_size} in \code{\link[=robust_rtm]{robust_rtm()}}: clusters at
least as wide as the range contain essentially all of the residual
correlation.
}
\details{
For each band \eqn{(d_{k-1}, d_k]}, cell pairs whose centroid distance
falls in the band define the (row-standardized) spatial weights, and
Moran's I is tested against \code{nsim} random permutations of the residuals
(one-sided, positive). The estimated range is the upper edge of the
initial consecutive run of significant bands: correlation beyond a gap
of non-significant bands is treated as noise rather than an extension of
the range. As in \code{\link[=moran_rtm]{moran_rtm()}}, offset models use only the cells that
entered the fit.

By default the bands step by one cell width up to three times the
largest candidate spatial influence (at most 15 bands). All cell pairs
within the largest band distance are enumerated, so very large grids
combined with wide bands can be memory-hungry; reduce \code{max(breaks)} if
needed.
}
\references{
Cliff, A. D., & Ord, J. K. (1981). \emph{Spatial Processes:
Models and Applications}. London: Pion.
}
\seealso{
\code{\link[=moran_rtm]{moran_rtm()}} for the single overall test, \code{\link[=robust_rtm]{robust_rtm()}} for
the cluster-robust inference the result feeds into.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/moran_rtm.R
\name{moran_rtm}
\alias{moran_rtm}
\title{Test model residuals for spatial autocorrelation with Moran's I}
\usage{
moran_rtm(
  x,
  type = c("pearson", "deviance"),
  nsim = 999,
  alternative = c("greater", "less", "two.sided")
)
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{type}{Residual type passed to \code{\link[stats:residuals.glm]{stats::residuals.glm()}}:
\code{"pearson"} (default) or \code{"deviance"}.}

\item{nsim}{Number of random permutations for the null distribution
(default 999).}

\item{alternative}{Alternative hypothesis: \code{"greater"} (positive
autocorrelation, the default and the typical concern), \code{"less"}, or
\code{"two.sided"}.}
}
\value{
An object of class \code{rtm_moran}: a list with \code{statistic}
(observed Moran's I), \code{expected} (its null expectation, -1/(n-1)),
\code{p_value} (permutation p-value), \code{alternative}, \code{nsim}, \code{sims} (the
permuted statistics), \code{n_cells} (cells used), \code{n_dropped} (cells
without neighbours), and \code{type}.
}
\description{
Computes Moran's I on the residuals of a fitted \code{\link[=rtm]{rtm()}} model and tests
it against the null hypothesis of no spatial autocorrelation by random
permutation of the residuals across cells.
}
\details{
The RTMDx procedure (and hence \code{\link[=rtm]{rtm()}}) treats grid cells as independent
observations. Heffner (2013) acknowledges that spatial autocorrelation is
not modeled and that significance values are inaccurate in its presence;
left unchecked it also makes the cross-validated culling optimistic and
weakens BIC's complexity penalty, both of which push towards selecting
too many variables. This diagnostic makes the assumption checkable:
residual spatial clustering that the selected risk factors have \emph{not}
absorbed shows up as a positive Moran's I with a small permutation
p-value.

Neighbours are defined by queen contiguity (cells sharing at least a
corner) with row-standardized weights. For offset models the test uses
only the cells that entered the fit (non-zero denominator); contiguity is
then evaluated among those cells, and cells without any fitted neighbour
are dropped from the statistic with a message.

A significant result does not invalidate the risk terrain map, but it
means reported p-values are anti-conservative and model selection may be
too liberal. Recommended follow-up: refit the selected model with a
smooth spatial term (e.g. \code{mgcv::gam(... + s(x, y))}) and report the
stability of the RRVs.
}
\references{
Moran, P. A. P. (1950). Notes on continuous stochastic
phenomena. \emph{Biometrika}, 37, 17-23.

Cliff, A. D., & Ord, J. K. (1981). \emph{Spatial Processes: Models and
Applications}. London: Pion.

Heffner, J. (2013). Statistics of the RTMDx Utility. In J. Caplan, L.
Kennedy, & E. Piza, \emph{Risk Terrain Modeling Diagnostics Utility User
Manual (Version 1.0)}. Newark, NJ: Rutgers Center on Public Security.
}
\seealso{
\code{\link[=rtm]{rtm()}}, \code{\link[=validate_rtm]{validate_rtm()}}
}

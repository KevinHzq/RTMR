% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rtm_raster.R
\name{rtm_raster}
\alias{rtm_raster}
\title{Convert a risk terrain model to a raster}
\usage{
rtm_raster(x, what = c("relrisk", "prediction", "outcome_count"))
}
\arguments{
\item{x}{An \code{rtm} object.}

\item{what}{Cell value to rasterize: \code{"relrisk"} (relative risk score,
default; RTMDx's "output_score"), \code{"prediction"} (expected event count;
RTMDx's "output_prediction"), or \code{"outcome_count"}.}
}
\value{
A \link[terra:SpatRaster]{terra::SpatRaster} with one layer.
}
\description{
Rasterizes the grid of a fitted \code{\link[=rtm]{rtm()}} model so the risk terrain map can
be used with raster tools (map algebra, comparison of risk surfaces) or
exported to GIS software. Cells outside the study area are \code{NA}, matching
the masking behaviour of the RTMDx Utility's GeoTiff outputs.
}
\details{
Requires the terra package. The raster resolution is derived from the
grid cell area, which is exact for the default square grid; hexagonal
grids are rasterized at an approximately equivalent resolution.
}
\seealso{
\code{\link[=write_rtm]{write_rtm()}} to write directly to a GeoTiff file.
}

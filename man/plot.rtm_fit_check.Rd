% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_fit.R
\name{plot.rtm_fit_check}
\alias{plot.rtm_fit_check}
\title{Plot goodness-of-fit diagnostics for a risk terrain model}
\usage{
\method{plot}{rtm_fit_check}(x, which = c("rootogram", "qq"), max_count = NULL, ...)
}
\arguments{
\item{x}{An \code{rtm_fit_check} object from \code{\link[=check_fit]{check_fit()}}.}

\item{which}{Plots to draw: \code{"rootogram"}, \code{"qq"}, or both (default;
drawn side by side).}

\item{max_count}{Largest count value shown in the rootogram (default:
the maximum observed count).}

\item{...}{Passed on to \code{\link[=plot]{plot()}}.}
}
\description{
Draws the two standard visual fit checks for count models from a
\code{\link[=check_fit]{check_fit()}} result:
}
\details{
\itemize{
\item \strong{Hanging rootogram} (Kleiber & Zeileis, 2016): for each count value
0, 1, 2, ... the square root of the observed cell frequency hangs as
a bar from the square root of the model-expected frequency. Bars
dangling below the zero line are counts the model \emph{under}-predicts;
bars stopping short of it are counts it \emph{over}-predicts. Zero excess
shows as the 0-bar punching below the line; overdispersion as a
sagging middle with heavy tails.
\item \strong{Quantile-residual Q-Q plot} (Dunn & Smyth, 1996): randomized
quantile residuals are standard normal when the model is correct, so
departures from the diagonal read like an ordinary Q-Q plot. The
randomization draws from the RNG; call \code{set.seed()} first for a
reproducible figure.
}
}
\references{
Kleiber, C., & Zeileis, A. (2016). Visualizing count data
regressions using rootograms. \emph{The American Statistician}, 70,
296-303.

Dunn, P. K., & Smyth, G. K. (1996). Randomized quantile residuals.
\emph{Journal of Computational and Graphical Statistics}, 5, 236-244.
}
